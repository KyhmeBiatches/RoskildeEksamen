import React from 'react'
import ReactDOM from 'react-dom'

class SearchPager extends React.Component {
    render(){
        return (
            <div><p>SearchPager</p></div>
        );
    }
}

module.exports = SearchPager;
window.SearchPager = SearchPager;